<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="vpiTabPageIndicatorStyle">@style/Widget.TabPageIndicator</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="vpiTabPageIndicatorStyle">@style/Widget.TabPageIndicator</item>
    </style>

    <style name="ToolbarStyle" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:background">@color/lightBlue</item>
        <item name="popupTheme">@style/ThemeOverlay.AppCompat.Light</item>
    </style>

    <!-- Style the tab bar (e.g. on Feed fragment), and give child tabs a text appearance -->
    <style name="AppTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabBackground">@color/barBackground</item>
        <item name="tabIndicatorColor">@color/lightBlue</item>
        <item name="tabSelectedTextColor">@color/white</item>
        <item name="tabTextAppearance">@style/TabTextAppearance</item>
    </style>

    <style name="TabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="PositiveButton" parent="Widget.AppCompat.Button.Borderless">
        <item name="colorButtonNormal">#52BB42</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- Login / Sign Up -->
    <style name="LoginSignUpHeadingTextAppearance">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="LoginHeadingTextAppearance" parent="LoginSignUpHeadingTextAppearance">
        <item name="android:textSize">33sp</item>
    </style>

    <style name="SignUpHeadingTextAppearance" parent="LoginSignUpHeadingTextAppearance">
        <item name="android:textSize">25sp</item>
    </style>

    <style name="LoginSignUpButton" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="LoginButton" parent="LoginSignUpButton">
        <item name="colorButtonNormal">#52BB42</item>
    </style>

    <style name="SignUpButton" parent="LoginSignUpButton">
        <item name="colorButtonNormal">#4E8DDC</item>
    </style>

    <style name="LoginSignUpSmallPrintText">
        <item name="android:textColor">@color/white</item>
        <item name="android:alpha">0.7</item>
    </style>

    <!-- Self Efficacy Questions -->
    <style name="QuestionSubheadingTextAppearance">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">16sp</item>
    </style>

</resources>
