<?xml version="1.0" encoding="utf-8"?>

<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <item
        android:id="@+id/candidate_profile_add_friend"
        android:title="@string/election_candidate_profile_add_friend"
        android:icon="@drawable/add_friend_menu_option"
        app:showAsAction="always"
        tools:iconTint="@color/white"
        android:tooltipText="@string/election_candidate_profile_add_friend"/>

    <item
        android:id="@+id/candidate_profile_remove_friend"
        android:title="@string/election_candidate_profile_remove_friend"
        android:icon="@drawable/remove_friend_menu_option"
        app:showAsAction="always"
        tools:iconTint="@color/white"
        android:tooltipText="@string/election_candidate_profile_remove_friend"/>
</menu>